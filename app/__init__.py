# import model for alembic

from app.users.models import User, Group, UserRole, UserGroupRole, Event, Attendance, EventGroup, Notification
from app.users.models import StatusEnum, WeekEnum, CourseEnum
